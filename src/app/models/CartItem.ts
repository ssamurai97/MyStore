export type CartItem = {
    name?: string,
    price?: number,
    quantity?: number,
    url?: string,
}

export type User = {
    fullName?: string,
    address?: string,
    creditcard?: number,

}
